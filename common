REPO_BASE="http://pp.info.uni-karlsruhe.de/git"
LIBFIRM_REPO="${REPO_BASE}/libfirm.git"
CPARSER_REPO="${REPO_BASE}/cparser.git"
MAKEFLAGS="-j1"

if test -e common_override; then
	source common_override
fi
